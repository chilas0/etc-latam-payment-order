spring:
  application:
    name: payment-ms

  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: ${SPRING_KAFKA_CONSUMER_GROUP_ID:payment-ms-group}
      auto-offset-reset: earliest

server:
  port: ${SERVER_PORT:8083}